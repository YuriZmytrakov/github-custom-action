name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
outputs:
  greeting:
    description: 'How we greet'
runs:
  using: 'node20'
  main: 'index.js'
